# PLAN.md: แผนงาน Sprint 1 (Front-End App Dev)

**โปรเจกต์:** ระบบค้นพบภาพยนตร์และสร้างรายการรับชม (Movie Discovery & Watchlist Builder)
**รายวิชา:** CP352301 Script Programming (1/2569) — Final Project
**Sprint 1:** สัปดาห์ที่ 12 (นำเสนอ 15–16/9/69, ส่งงาน 18/9/69)
**สถานะ:** กำหนดสเปกต์และ Definition of Done เรียบร้อยแล้วก่อนเริ่มเขียนโค้ด

---

## 1. เป้าหมายของ Sprint 1

Sprint นี้เน้นการพัฒนาส่วนติดต่อกับผู้ใช้ (Presentation Layer) ของโปรแกรม ให้ผู้ใช้สั่งงานผ่านเมนูคำสั่งได้สะดวก ประกอบด้วย

- ข้อความต้อนรับ (welcome banner) และเมนูคำสั่ง (menu)
- การรับคำสั่งและแปลงอินพุต โดยตัดช่องว่างและแปลงเป็นตัวพิมพ์เล็กด้วย `.strip().lower()`
- การตรวจสอบความถูกต้องของข้อมูลนำเข้า (Input Validation) ครอบคลุมกรณีขอบเขต
- การควบคุมการทำงานหลักด้วย `while` loop และจัดการข้อผิดพลาดด้วย `try / except`

## 2. ขอบเขต (Scope)

| ประเภท | รายการ |
|---|---|
| ในขอบเขต | โปรแกรม CLI ที่รับคำสั่งจากผู้ใช้ มีเมนู การตรวจสอบอินพุต การจัดการข้อผิดพลาด และชุดทดสอบอัตโนมัติของ CLI |
| นอกขอบเขต (Sprint 2 เป็นต้นไป) | การเชื่อมต่อ TMDB API จริง ฐานข้อมูล SQLite อัลกอริทึมค้นหา/กรอง/เรียงลำดับจริง และการส่งออก CSV |
| หมายเหตุ | ใน Sprint นี้ คำสั่ง search / discover สาธิตด้วยข้อมูลตัวอย่างที่ฝังไว้ในโปรแกรมก่อน เพื่อให้ทดสอบส่วนติดต่อกับผู้ใช้ได้เสร็จก่อนเชื่อมระบบจริง |

## 3. คำสั่งของระบบ (Command Specification)

| คำสั่ง | อินพุต | พฤติกรรมที่คาดหวัง |
|---|---|---|
| `search <ชื่อเรื่อง>` | ข้อความ | ค้นหาภาพยนตร์จากชื่อเรื่องด้วยข้อมูลตัวอย่าง ถ้าไม่พบจะแจ้งให้ทราบ |
| `discover <รหัส>` | จำนวนเต็มมากกว่า 0 | แสดงรายชื่อภาพยนตร์ที่คล้ายกัน เรียงตามคะแนนจากมากไปน้อย |
| `watchlist add <รหัส>` | จำนวนเต็มมากกว่า 0 | เพิ่มภาพยนตร์เข้ารายการรับชม ไม่เพิ่มซ้ำ |
| `watchlist list` | — | แสดงรายการรับชมปัจจุบัน |
| `watchlist clear` | — | ล้างรายการรับชม |
| `help` | — | แสดงคำสั่งทั้งหมด |
| `quit` / `exit` / `q` / `ออก` | — | ออกจากโปรแกรมทันที ไม่ว่าพิมพ์แบบใด |

## 4. Definition of Done (DoD)

- [ ] พิมพ์ `quit` แบบตัวพิมพ์เล็ก ตัวพิมพ์ใหญ่ หรือมีช่องว่างหน้า-หลัง โปรแกรมต้องออกทันทีพร้อมข้อความอำลา
- [ ] พิมพ์คำสั่งที่ไม่รู้จัก โปรแกรมต้องแจ้งเตือนและกลับสู่เมนู ไม่หยุดทำงาน
- [ ] กด Enter โดยไม่พิมพ์อะไร โปรแกรมต้องแจ้งเตือน "กรุณาพิมพ์คำสั่ง"
- [ ] ป้อนรหัสภาพยนตร์ที่ไม่ใช่ตัวเลข (เช่น `abc`) โปรแกรมต้องแจ้งเตือนโดยไม่หยุดทำงาน ตามหลักการ `try / except ValueError`
- [ ] ป้อนรหัสภาพยนตร์ติดลบหรือศูนย์ (`-3`, `0`) โปรแกรมต้องปฏิเสธพร้อมข้อความแจ้ง
- [ ] อินพุตสิ้นสุด (EOF) หรือกด Ctrl+C โปรแกรมต้องออกอย่างสุภาพ
- [ ] โค้ดต้องผ่านการตรวจด้วย flake8 และทุกเมธอดต้องมี docstring

## 5. บทบาททีม (หมุนเวียนตามรอบ Sprint)

| บทบาท | ผู้รับผิดชอบ |
|---|---|
| Planner / Team Leader | [ชื่อนักศึกษา] |
| Coder | นายอเสข ปัญญาวงค์ |
| Debugger / QA | [ชื่อนักศึกษา] |

## 6. แผนการทดสอบ (Test Plan)

ทดสอบกรณีขอบเขตตาม Definition of Done ด้วยสองช่องทาง

1. ชุดทดสอบอัตโนมัติด้วย `pytest` ซึ่งจำลองอินพุตทั้งหมด ไม่ต้องใช้งานผ่าน terminal จริง
2. การจำลองเซสชันการใช้งานจริงด้วยอินพุตสคริปต์ สรุปผลไว้ในรายงาน Sprint 1 (notebook)
